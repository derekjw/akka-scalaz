akka {

  actor {
    timeout = 30
    default-dispatcher {
      #type = "ExecutorBasedEventDriven"
      #core-pool-size-factor = 1.0
      #max-pool-size-factor  = 1.0
    }
  }

  scalaz {
    executer = "hawt" # use "hawt" to use HawtDispatch directly
  }
}
