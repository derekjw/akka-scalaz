akka {
  version = "1.0-RC3"

  actor {
    timeout = 30
    default-dispatcher {
      #type = "ExecutorBasedEventDriven"
      #core-pool-size-factor = 1.0
      #max-pool-size-factor  = 1.0
    }
  }

  scalaz {
    executer = "akka" # use "hawt" to use HawtDispatch directly
  }
}
